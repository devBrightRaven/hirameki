name: Validate Commands

on:
  push:
    paths:
      - "commands/**"
      - "codex/**"
      - "tests/**"
  pull_request:
    paths:
      - "commands/**"
      - "codex/**"
      - "tests/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate command files
        run: python tests/validate_commands.py

      - name: Validate Codex skill adapter
        run: python tests/validate_codex_skill.py

      - name: Smoke test Hirameki workflows
        run: python tests/smoke_hirameki_workflows.py
